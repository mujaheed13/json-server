const baseUrl = "http://localhost:3000/";
const productsData = "http://localhost:3000/api/products";
const usersData = "http://localhost:3000/api/users";

// steps to use server :-
// => install node modules in backend folder [npm i];
// => run "node server.js" command in the terminal;


// -------------------------------------------------

// To add the data use => POST method

// To edit or update data 
// use => PATCH method

// To delete use => DELETE 
